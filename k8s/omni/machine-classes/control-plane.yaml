metadata:
  namespace: default
  type: MachineClasses.omni.sidero.dev
  id: libvirt-controlplane
spec:
  autoprovision:
    providerid: libvirt
    providerdata: |
      cores: 2
      memory: 4096 # MB
      disk_size: 40
      storage_pool: "tank-vm"
      network_interfaces:
        - driver: bridge
          network_name: "br31"
