apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-library
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce # required — shared between server + machine-learning pods (ReadWriteMany)
  resources:
    requests:
      storage: 150Gi
  storageClass: zfs-iscsi
