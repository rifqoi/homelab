apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rifqoi-com-tls
  namespace: immich
spec:
  secretName: rifqoi-com-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.rifqoi.com" # wildcard covers all subdomains
